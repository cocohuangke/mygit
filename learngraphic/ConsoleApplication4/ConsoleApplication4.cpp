#include <iostream>
#include<Cstdio>

int main()
{
	double x;
	int a, b, c;
	scanf_s("%d", &a);
	scanf_s("%d", &b);
	scanf_s("%d", &c);
	double delta = b * b - 4 * a * c;
	if (delta > 0) {
		root1=
	}

}
